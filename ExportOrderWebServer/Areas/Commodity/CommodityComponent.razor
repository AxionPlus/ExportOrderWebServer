
@inject IDialogService DialogService

<style>

    .filters {
        display: flex;
        width: 100%;
        align-items: center;
        padding-bottom:25px;
    }

    .min-width {
        width: 960px;
    }
</style>

<div class="@(MudDialog is null ? "item-container": "item-container min-width")">

    <div class="container-head"></div>
    <div class="container-content">
        <MudButton Class="wrapper-button" Variant="Variant.Filled" Color="Color.Primary"
                   OnClick="@(async ()=>{newItemBtnDisable=true; await NewItem_Click(); await Task.Delay(1000); newItemBtnDisable=false; })">New Commodity</MudButton>
    </div>

</div>

<div class="item-container">

    <div class="container-head"></div>
    <div class="container-content column ">

        <div class="filters">

            <MudTextField Class="wrapper-input" @bind-Value="filter_HSCode"
                          Label="HS Code"
                          Mask="@(new PatternMask("0000000000"))"
                          MaxLength="10" Variant="Variant.Outlined" Clearable></MudTextField>
            <MudSpacer />
            <MudTextField Class="wrapper-input" @bind-Value="filter_NAME" Label="NAME" Variant="Variant.Outlined" Clearable></MudTextField>
            <MudSpacer />
            <MudTextField Class="wrapper-input" @bind-Value="filter_EngNAME" Label="Eng NAME" Variant="Variant.Outlined" Clearable></MudTextField>

            <MudSpacer />
            <MudButton Class="wrapper-button" Variant="Variant.Filled" Color="Color.Secondary" Disabled="@searchBtnDisable"
                       OnClick="@(async ()=>{searchBtnDisable=true; await SearchItems_Click(); await Task.Delay(1000); searchBtnDisable=false; })">Search</MudButton>
        </div>
        <div class="viewDataTable">
            <MudTable Items="@Items" Loading="searchBtnDisable">
                <HeaderContent>
                    <MudTh Style="width:30px"></MudTh>
                    <MudTh Style="width:100px">HS Code</MudTh>
                    <MudTh >Name</MudTh>
                    <MudTh Style="width:200px">Eng name</MudTh>

                </HeaderContent>
                <RowTemplate>
                    <MudTd><MudIconButton Icon="@Icons.Material.Filled.Check" OnClick="@(()=>{SelectItem(context);})"></MudIconButton></MudTd>
                    <MudTd DataLabel="Terminal">@context.HSCode</MudTd>
                    <MudTd DataLabel="Name">@context.Name</MudTd>
                    <MudTd DataLabel="VoyageCarrier">@context.EngName</MudTd>

                </RowTemplate>
                <PagerContent>
                    <MudTablePager />
                </PagerContent>
            </MudTable>
        </div>
    </div>
</div>

@code {
    [CascadingParameter] MudDialogInstance? MudDialog { get; set; }
    bool newItemBtnDisable = false;
    bool searchBtnDisable = false;

    string? filter_HSCode;
    string? filter_NAME;
    string? filter_EngNAME;


    [Parameter]
    public IEnumerable<CommodityCatalog>? Items { get; set; } = new List<CommodityCatalog>();

    private async Task SearchItems_Click()
    {
        //Items = await Http.GetFromJsonAsync<IEnumerable<CommodityCatalog>>("api/DictionaryData/Commodities");

        //if (Items is null)
        //    return;


        //if (!string.IsNullOrWhiteSpace(filter_HSCode))
        //    Items = Items.Where(s => s.HSCode.Contains(filter_HSCode)).ToArray();

        //if (!string.IsNullOrWhiteSpace(filter_NAME))
        //    Items = Items.Where(s => s.Name.Contains(filter_NAME)).ToArray();

        //if (!string.IsNullOrWhiteSpace(filter_EngNAME))
        //    Items = Items.Where(s => s.EngName.Contains(filter_EngNAME)).ToArray();



    }


    private async Task NewItem_Click()
    {
        //var parameters = new DialogParameters { ["server"] = server };

        var options = new DialogOptions { CloseButton = true, MaxWidth = MaxWidth.Large, DisableBackdropClick = true };
        var dialog = await DialogService.ShowAsync<ItemCommodityComponent>("NEW COMMODITY", options);
        var result = await dialog.Result;

        if (!result.Canceled)
            if (MudDialog is not null)
            {

                MudDialog?.Close(DialogResult.Ok(result.Data));

            }
    }


    private async Task SelectItem(CommodityCatalog entity)
    {

        if (MudDialog is not null)
        {
            MudDialog.Close(DialogResult.Ok(entity));
            return;
        }

        var parameters = new DialogParameters() { ["Item"] = entity };
        var options = new DialogOptions { CloseButton = true, MaxWidth = MaxWidth.Large, DisableBackdropClick = true };
        var dialog = await DialogService.ShowAsync<ItemCommodityComponent>($"COMMODITY HS code  {entity.HSCode}", parameters, options);
        var result = await dialog.Result;

        if (!result.Canceled)
            if (MudDialog is not null)
            {

            

            }


    
    }
}
