@inherits LayoutComponentBase
@inject AuthenticationStateProvider authenticationStateProvider;

<PageTitle>Export Order</PageTitle>

<MudThemeProvider />
<MudDialogProvider />
<MudSnackbarProvider />

<MudLayout>
    <MudDrawer @bind-Open="@_drawerOpen" ClipMode="@DrawerClipMode.Never" Variant="@DrawerVariant.Temporary" Anchor="@Anchor.Start">
        <NavMenu />
    </MudDrawer>
    <MudContainer MaxWidth="MaxWidth.Large" Style="padding-bottom: 15px;">
        <CascadingValue Value=ShowMainMenu>
            @* <AuthorizeView> *@
                @* <Authorized> *@
                    @Body
                @* </Authorized> *@
@*                 <NotAuthorized>
                    <LoginRedirect />
                </NotAuthorized> *@
            @* </AuthorizeView> *@
        </CascadingValue>
    </MudContainer>
</MudLayout>


@code {
    [CascadingParameter] MudDialogInstance? MudDialog { get; set; }

    EventCallback ShowMainMenu => EventCallback.Factory.Create(this, ToggleDrawer);


    bool _drawerOpen = false;

    void ToggleDrawer()
    {
        _drawerOpen = !_drawerOpen;
    }

    private async Task LogoutApp()
    {
        // NavigationManager.NavigateTo($"{NavigationManager.BaseUri}logoutUser");

    }
    // protected override async Task OnInitializedAsync()
    // {
    //     var authState = await authenticationStateProvider.GetAuthenticationStateAsync();
    //     ApplicationParameter.ApplicationUser = authState.User.Identity.Name;

    // }
}